{
  "name": "url-shortener",
  "version": "1.0.0",
  "scripts": {
    "client": "cd ../frontend && npm start",
    "server": "nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"set NODE_ENV=development && npm run server\" \"npm run client\"",
    "test": "mocha"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "http-status-codes": "^2.1.2",
    "mysql": "^2.18.1",
    "nodemon": "^2.0.4",
    "mocha": "^8.1.3",
    "chai": "^4.2.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
