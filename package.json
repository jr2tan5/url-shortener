{
  "name": "url-shortener",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd frontend && npm install && cd ../backend && npm install && npm run build"
  }
}
