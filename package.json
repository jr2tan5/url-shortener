{
  "name": "url-shortener",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && npm run server",
    "heroku-prebuild": "cd frontend && npm install && cd ../backend && npm install",
    "heroku-postbuild": "cd backend && npm run test && ../frontend npm run build",
    "dev": "cd backend && npm run dev",
    "test": "cd backend && npm run test"
  }
}
